services:
  rag:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./metrics_log.jsonl:/app/metrics_log.jsonl
      - ./pdfs:/app/pdfs
    restart: unless-stopped
